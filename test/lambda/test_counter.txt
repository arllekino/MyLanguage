func makeCounter(): (): Int {
    let count = 0
    let counter = (): Int {
        count = count + 1
        return count
    }
    return counter
}

let c = makeCounter()
print(c()) // 1
print(c()) //  2