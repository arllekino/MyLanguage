import "libs/UI"

struct MyView: View {
    let view: any View {
        return VBox(16.0, HAlign.center) {
            return [
                Text("Hello, Rocket UI!", 24.0, Color.white)
            ]
        }
    }
}

let app = App()

app.run("Text Test", 800, 600) {
    return MyView()
}
