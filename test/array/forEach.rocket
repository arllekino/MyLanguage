let numbers = [1, 2, 3, 4, 5]

print(numbers.count) // 5

// forEach с неявным it
numbers.forEach {
    print(it) // 1 2 3 4 5
}

// filter + map
let evens = numbers.filter { it % 2 == 0 }
let doubled = evens.map { it * 2 }

doubled.forEach {
    print(it) // 4, 8
}